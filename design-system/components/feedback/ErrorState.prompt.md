Ошибка. Всегда говорим, что данные сохранены, и даём одно действие.

```jsx
<ErrorState action={<Button variant="secondary" size="sm">Попробовать ещё раз</Button>} />
<ErrorState variant="screen" title="Нет связи" description="Проверьте интернет — расклад ждёт вас здесь." />
```

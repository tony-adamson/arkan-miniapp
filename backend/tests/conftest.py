"""Общие фикстуры тестов.

Настройки читаются из `infra/env.test` (его подставляет `make test`), поэтому
отдельных подмен окружения в фазе 1 не требуется; фикстуры БД, Redis и Qdrant
появляются в фазах 2, 8 и 16.
"""
